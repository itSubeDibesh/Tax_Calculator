import Inputs from "../inputs/index.js";
import { elements } from '../../elements/index.js';

const {
  
} = elements;


export default class Calculations extends Inputs {
    constructor() {
        super(Inputs);
    }
}